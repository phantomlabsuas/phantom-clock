# Python
__pycache__/
*.py[cod]

# PyInstaller build artifacts (generated, not source)
packager/build/
packager/phantom-clock.spec
packager/win_version_gen.txt

# Release output — rebuilt by build script, not tracked
release/

# Crash logs written at runtime
phantom-clock-crash.log
